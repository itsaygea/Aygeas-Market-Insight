name: Build and Release

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    env:
      DALAMUD_HOME: ${{ github.workspace }}/dalamud
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Download Dalamud
        shell: bash
        run: |
          curl -sL https://goatcorp.github.io/dalamud-distrib/latest.zip -o dalamud.zip
          unzip dalamud.zip -d "$DALAMUD_HOME"

      - name: Build
        run: dotnet build --configuration Release

      - name: Package
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          mkdir -p staging
          cp bin/Release/AygeaMarketInsight.dll staging/
          cp bin/Release/AygeaMarketInsight.json staging/
          cp bin/Release/icon.png staging/
          cd staging && 7z a ../latest.zip ./*

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: latest.zip
          generate_release_notes: true
